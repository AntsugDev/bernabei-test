{% extends 'base.html.twig' %}
{% block title %}
    Error
{% endblock %}
{% block body %}
    <div class="wrapper">
        <div class="form-signin">
            <p style="font-weight: 800;text-align: center;font-size: 16px">{{ status }}</p>
            <p>{{ msg }}</p>
            <a href="/" class="btn btn-warning" style="color: #fff">Torna alla login</a>
        </div>
    </div>
{% endblock %}

